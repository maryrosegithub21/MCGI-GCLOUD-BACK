steps:
  # Step to build the backend application using Buildpacks
  - name: 'gcr.io/k8s-skaffold/pack'
    args: ['build', '--builder=gcr.io/buildpacks/builder:v1', '--path=.', '--tag=gcr.io/$PROJECT_ID/mcgi-gcloud-back']

  # Step to deploy the backend application to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args: ['run', 'deploy', 'mcgi-gcloud-back', '--image=gcr.io/$PROJECT_ID/mcgi-gcloud-back', '--region=us-central1', '--platform=managed']

images:
  - 'gcr.io/$PROJECT_ID/mcgi-gcloud-back'